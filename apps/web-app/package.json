{
  "name": "@osdiab-website/web-app",
  "description": "Demo App with Routing built-in (recommended)",
  "engines": {
    "node": "^18.17.0 || ^20.3.0 || >=21.0.0"
  },
  "engines-annotation": "Mostly required by sharp which needs a Node-API v9 compatible runtime",
  "private": true,
  "trustedDependencies": [
    "sharp"
  ],
  "trustedDependencies-annotation": "Needed for bun to allow running install scripts",
  "type": "module",
  "scripts": {
    "build": "qwik build",
    "build.client": "vite build",
    "build.preview": "vite build --ssr src/entry.preview.tsx",
    "build.server": "vite build -c adapters/cloudflare-pages/vite.config.ts",
    "build.types": "tsc --incremental --noEmit",
    "deploy": "wrangler pages deploy ./dist",
    "dev": "bun run-p dev:* dev.normal:server",
    "dev:pandacss": "panda --watch",
    "dev.normal:server": "bun vite --mode ssr",
    "dev.debug": "bun run-p dev:* dev.debug:server",
    "dev.debug:server": "bun run --bun --inspect-brk vite --mode ssr --force",
    "prebuild.pandacss": "panda codegen --silent",
    "preview": "qwik build preview && vite preview --open",
    "serve": "wrangler pages dev ./dist --compatibility-flags=nodejs_als",
    "start": "vite --open --mode ssr",
    "qwik": "qwik"
  },
  "devDependencies": {
    "@builder.io/qwik": "^1.5.2",
    "@builder.io/qwik-city": "^1.5.2",
    "@builder.io/vite-plugin-macro": "~0.0.7",
    "@pandacss/dev": "^0.37.2",
    "@types/node": "^20.12.7",
    "npm-run-all2": "^6.1.2",
    "typescript": "5.4.5",
    "undici": "*",
    "vite": "^5.2.10",
    "vite-tsconfig-paths": "^4.3.2",
    "wrangler": "^3.51.2"
  }
}
